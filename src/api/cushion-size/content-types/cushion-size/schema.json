{
  "kind": "collectionType",
  "collectionName": "cushion_sizes",
  "info": {
    "singularName": "cushion-size",
    "pluralName": "cushion-sizes",
    "displayName": "Cushion Size"
  },
  "options": { "draftAndPublish": true },
  "attributes": {
    "name": { "type": "string", "required": true },
    "width_cm": { "type": "integer", "required": true },
    "height_cm": { "type": "integer", "required": true },
    "shape": {
      "type": "enumeration",
      "enum": ["square", "rectangular", "bolster", "round", "oval"],
      "required": true
    },
    "workmanship_cost": {
      "type": "decimal",
      "default": 25,
      "description": "Per-cushion workmanship charge used by the cushion pricing rule"
    },
    "duck_feather_surcharge": {
      "type": "decimal",
      "default": 0,
      "description": "Legacy fallback for duck-feather pad rows whose pad price is null"
    },
    "thumbnail": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"]
    }
  }
}
